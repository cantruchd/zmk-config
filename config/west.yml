manifest:
  defaults:
    revision: v0.3
  remotes:
    #- name: zmkfirmware
     # url-base: https://github.com/zmkfirmware
    # Additional modules containing boards/shields/custom code can be listed here as well
    # See https://docs.zephyrproject.org/3.2.0/develop/west/manifest.html#projects
    - name: cantruchd #new entry
      url-base: https://github.com/cantruchd #new entry
    # - name: pbogut #new entry
    #   url-base: https://github.com/pbogut #new entry
  projects:
    - name: zmk
      #remote: zmkfirmware
      remote: cantruchd
      import: app/west.yml
      revision: mainfromzmk #new entry
      # 2. KHAI BÁO MODULE WIDGET CUSTOM BẰNG URL CỤC BỘ (LƯU Ý: file://...)
    # Cách này giúp West thỏa mãn yêu cầu về URL/remote mà không cần remote ngoài
    - name: nice_view_custom_widgets
      # SỬ DỤNG 'url' với giao thức 'file://' 
      # Trỏ đến thư mục hiện tại của repository (. - dấu chấm)
      url: file://. 
      path: app/nice_view_custom_widgets
      # West cần revision cho mọi project, bạn có thể dùng một giá trị cố định hoặc revision của repo chính
      revision: addtemp # Sử dụng revision của repo chính (nếu không rõ, dùng 'main')
  self:
    path: config
